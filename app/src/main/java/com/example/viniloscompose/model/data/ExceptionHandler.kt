package com.example.viniloscompose.model.data
import com.example.viniloscompose.utils.IExceptionHandler

class ExceptionHandler : IExceptionHandler {
    override fun handle(t: Throwable) {
        // Handle the throwable here. This could involve logging the throwable,
        // showing an error message to the user, etc.
    }
}